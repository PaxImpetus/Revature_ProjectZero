package scala.Main

object UserInteaction{
    def execFunc(input:String):Unit={
        // author code to execute the user functionality here
        // recall that the main.scala file should ONLY operate as the entry point, with minimal logic
        // This part is totally optional, the whole "what it does" is up to you
    }
}